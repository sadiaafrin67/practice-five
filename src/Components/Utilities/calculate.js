const add = (first, second) => {
    return first + second
}

const multiply = (first, second) => {
    return first * second
}

const firstNumberDivitedBySecondNumber = (first, second) => {
    return first / second
}

// export option 3
export {add, multiply, firstNumberDivitedBySecondNumber}

// export option 1
// export default add

// export option 2
// const calculator = {add: add}
// export default calculator